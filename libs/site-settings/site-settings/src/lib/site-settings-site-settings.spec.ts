import { siteSettingsSiteSettings } from './site-settings-site-settings';

describe('siteSettingsSiteSettings', () => {
  it('should work', () => {
    expect(siteSettingsSiteSettings()).toEqual('site-settings-site-settings');
  });
});
